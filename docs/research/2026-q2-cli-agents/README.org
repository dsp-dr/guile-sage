#+TITLE: 2026-Q2 CLI coding agents — local harness evaluation
#+AUTHOR: claude-agent (paired w/ jwalsh)
#+DATE: 2026-06-23
#+OPTIONS: toc:2 num:t ^:nil

Consolidation of the physical harness evaluation, extending the wal.sh
note (=SOURCE.org=, "CLI Coding Agents: 2026 Q2 Comparison"). For each
installed harness we capture *reference data* (CLI surface, slash
commands, docs/help, config) then *drive it* via a tmux session.

* Installed (mini, 2026-06-23)
claude, codex, gemini, pi, opencode, aider, crush.
(antigravity = Google, needs install: https://antigravity.google/download)

* Per-harness capture (saved under each =<harness>/= dir)
| File | What |
|------+------|
| =help.txt= | =<h> --help= (+ subcommand helps) |
| =version.txt= | =<h> --version= |
| =slash-commands.txt= | in-session =/= command list (driven via tmux) |
| =config.txt= | config file path + keys (=~/.<h>= etc.) |
| =drive.txt= | a real non-interactive run (trivial calc) + the transcript |
| =NOTES.org= | comparison-axis findings (below) |

* Comparison axes (from SOURCE.org)
invocation patterns · permission models · sandbox/isolation · MCP
support · hooks · skills/custom-agents · memory/persistence · custom
model providers · remote/cloud execution · telemetry/OTEL.

* Signals
1. *general usage / familiarity* — Google chosen for easy API-key setup
   (antigravity download). Drive each, note ergonomics.
2. *contract honoring* — which honor OTEL vs DD (lapdog backfills:
   claude/codex/pi have native wrappers; see telemetry work).

* 2026 feature-review — broaden axes + use external catalogs (TODO)

Add for the 2026 review, and prefer existing overviews over hand-surveying:

- *Skills support* (new first-class axis). Multiple ecosystems now ship "skills":
  Claude Code skills, *GitHub Copilot / Microsoft "skills"*, etc. Compare how each
  harness defines/loads/scopes skills. Cross-ref guile-sage's own
  [[file:../../SKILLS-CONTRACT.org][SKILLS-CONTRACT.org]] + [[file:../../SKILLS-IMPL-SPEC.org][SKILLS-IMPL-SPEC.org]].
- *The landscape is ~50 harnesses — don't enumerate by hand.* Skill/rule
  *deployment* tools already target a huge agent matrix (their "select target
  agent(s)" pickers list ~50: GitHub Copilot, Claude Code, Cursor, Codex, Gemini
  CLI, Antigravity, Amp, Augment, Cline, Continue, Crush, Droid, Goose, Junie,
  Kilo/Kimi/Kiro/Kode, OpenCode, OpenHands, Pi, Qwen, Roo, Trae, Warp, Windsurf,
  Zencoder, …). *Action:* find + cite the tool(s) that produce these pickers
  (a rules/skills multi-target generator) and treat their agent list as the
  authoritative catalog; also reuse curated lists (awesome-agent-runtime-security,
  awesome-sandbox). We sample-deep only the harnesses we actually run.
- *Ollama wrapper / agent.* Ollama's launch (https://ollama.com/blog/launch) —
  evaluate whether it ships its own coding-agent/wrapper. Directly relevant:
  Ollama is guile-sage's default backend, so an Ollama-native agent is both a
  peer harness and a backend signal.

* Status
- [X] consolidation dir + source
- [ ] capture + drive each harness (in progress)
- [ ] cross-harness NOTES synthesis
- [ ] 2026 axes: skills support, external agent-catalog sources, Ollama wrapper (above)
